import { LoginForm } from "@/features/authentication/containers/LoginForm";
import React from "react";

const LoginPage = () => {
  return (
    <div className="h-screen flex items-center">
      <LoginForm />
    </div>
  );
};

export default LoginPage;

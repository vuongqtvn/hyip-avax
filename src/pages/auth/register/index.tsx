import { RegisterForm } from "@/features/authentication/containers/RegisterForm";

const RegisterPage = () => {
  return (
    <div className="h-screen flex items-center">
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
